# MSC resting-state scans: 10 x 206 MB, over GitHub's 100 MB per-file limit.
# Fetch from OpenNeuro ds000224 (derivatives/surface_pipeline) into data/msc/.
data/msc/

# Derived and reproducible: caches, simulation output, videos.
data/cache/
results/

# local tool config
.claude/

__pycache__/
*.pyc
.DS_Store
.ipynb_checkpoints/
.venv/
venv/

# RBC (ReproBrainChart) DataLad clones: NKI only. Large, and fetched on demand.
data/rbc/

# Ascending-input atlases and the group dMRI template. The manifest JSON and
# fib_source.txt in data/atlases/ stay tracked; the downloaded volumes do not.
data/atlases/aan/
data/atlases/harvardoxford/
data/atlases/thalamicseg/
data/atlases/dsistudio/
data/*.fib.gz

# Scratch notebook: 1.7 MB of stored cell output, and nothing else reads it.
*.ipynb
